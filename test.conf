// Test file for libconfig syntax highlighting

// Basic values
name = "test"
count: 42
enabled: true

// Group
server: {
    host = "localhost"
    port = 8080
}

// Array
ports: [ 80, 443, 8080 ]

// Include
@include "config.conf"
